# data.inc is regenerated by parser.lua at runtime (per-machine).
# A placeholder is committed so a fresh clone loads cleanly; uncomment the
# next line if you don't want your local generated copy to show as modified.
# ClaudeSessions/data.inc

# Runtime artifacts (normally written under ~/.claude/session-log, not here)
lua-error.txt
delete-error.txt
deleted.md

# Editor / OS cruft
*.bak
*.bak2
*.tmp
*.utf8.txt
Thumbs.db
desktop.ini
.DS_Store
